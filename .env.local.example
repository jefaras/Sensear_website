# SMTP Email Configuration
# Get these values from your web hosting provider

# SMTP Server hostname
SMTP_HOST=mail.sensear.music

# SMTP Port (usually 587 for TLS, 465 for SSL, or 25 for non-encrypted)
SMTP_PORT=587

# SMTP Authentication - Email account username (usually your full email address)
SMTP_USER=info@sensear.music

# SMTP Authentication - Email account password
SMTP_PASSWORD=your_password_here

# Email address to send FROM (must be authorized by your SMTP server)
SMTP_FROM=info@sensear.music

# Email addresses to send TO (comma-separated for multiple recipients)
SMTP_TO=info@sensear.music,jefaraz@gmail.com

# Optional: Use TLS/SSL (true or false)
SMTP_SECURE=false

# Optional: Company name for email templates
COMPANY_NAME=SensEar
