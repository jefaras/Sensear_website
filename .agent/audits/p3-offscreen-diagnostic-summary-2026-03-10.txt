Offscreen-lazy Diagnostic (2026-03-10)

Scope:
- Focused browser inspection for below-fold non-lazy images on:
  /en, /en/services, /en/blog, /en/contact, /en/about

Key finding:
- The shared footer assets are rendered below the fold with eager loading on multiple pages.
- The strongest recurring candidate for the single remaining offscreen-lazy warning is:
  /images/brand/sensear-logo-white.png
  (Footer brand logo in components/Footer.tsx)

Also repeatedly below-fold + eager in footer:
- /images/brand/contact-email.png
- /images/brand/sensear-logo-white.png (newsletter button variant width 20)

Interpretation:
- The scanner reports a singleton ("1 below-fold image(s) without lazy loading") across many pages,
  consistent with a shared footer image source.

Recommended next fix attempt:
1. Set footer brand logo (and optionally footer contact-email/newsletter logo) back to lazy/default.
2. Re-audit and verify whether offscreen-lazy clears without reintroducing lazy-above-fold on short pages.
